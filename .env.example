EXPO_PUBLIC_DATA_SOURCE=mock
EXPO_PUBLIC_API_BASE_URL=
# OIDC SSO (set both to switch the AuthProvider from demo to OIDC; redirect
# scheme defaults to the app scheme). Tokens will live in expo-secure-store.
EXPO_PUBLIC_OIDC_ISSUER=
EXPO_PUBLIC_OIDC_CLIENT_ID=
