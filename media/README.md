# WITS Mobile media

The short product tour uses screenshots captured from the synthetic prototype. The statistics in the final scene describe local code checks. They do not describe WCSD usage.

Run `pnpm install` from the repository root once. Run `pnpm --filter wits-mobile-media render` to create the video. The `poster` command creates a product image. The `stats` command creates an image of the verified development checks.
